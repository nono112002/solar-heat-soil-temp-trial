# Solar-Heat 用 Mosquitto 設定（GCE）

# MQTT（通常接続）
listener 1883
protocol mqtt

# WebSocket（ブラウザから接続用）
listener 9001
protocol websockets

# 認証
password_file /etc/mosquitto/passwd
allow_anonymous false

# 永続化はOS標準のパッケージデフォルト（/var/lib/mosquitto/）に任せる
